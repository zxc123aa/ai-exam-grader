下面是整合后的正式版计划文档，已经纳入 **AI 一键建立试卷模板、教师人工修正、自动判分、批注回写和后续 App 迁移**。

# 基于人工智能的试卷扫描与自动批卷系统开发计划

**文档版本：V1.0**
**首发形态：Web 系统**
**后续形态：移动端 App**
**预计 MVP 开发周期：14～16 周**

---

## 1. 项目概述

### 1.1 项目背景

教师批改纸质试卷通常需要完成以下重复工作：

* 翻阅和定位每一道题；
* 判断答案是否正确；
* 按评分点分配分数；
* 书写勾、叉、扣分原因和评语；
* 汇总学生总分；
* 统计班级错题和各题得分情况。

现有通用扫描软件主要解决纸质文档数字化问题，不能直接理解试卷题目结构，也不能完成题目识别、自动评分和卷面批注。

本项目拟开发一套面向教师的智能批卷系统，将纸质试卷扫描、试卷结构标定、答案识别、自动评分、教师复核和成绩统计整合为一套完整流程。

---

## 2. 项目目标

系统第一阶段以网页版为主，实现以下闭环：

```text
上传空白试卷和标准答案
        ↓
AI 一键建立试卷模板
        ↓
教师修正低置信度或错误区域
        ↓
批量上传学生答卷
        ↓
自动矫正、配准和裁题
        ↓
OCR、规则和大模型联合判分
        ↓
自动生成勾叉、得分和评语
        ↓
教师复核并确认
        ↓
导出批改试卷和成绩统计
```

项目第一阶段不以完全替代教师为目标，而是：

> 通过自动识别、评分建议、批注生成和异常题目筛选，显著减少教师的重复性批改工作，同时保证结果可解释、可修改、可追溯。

---

# 3. 产品定位

## 3.1 产品形态

第一阶段开发 Web 系统，主要面向教师在电脑端使用。

Web 端负责：

* 创建考试；
* 上传试卷；
* AI 建立模板；
* 检查和修正模板；
* 上传学生答卷；
* 查看识别和判分结果；
* 修改分数；
* 编辑批注；
* 导出试卷和成绩。

第二阶段开发手机 App，主要负责：

* 拍摄学生试卷；
* 自动寻找纸张边缘；
* 连续扫描；
* 模糊和曝光检测；
* 上传答卷；
* 查看批改结果。

OCR、判分、模板和批注逻辑仍由统一后端提供。

---

## 3.2 主要用户

第一阶段主要支持以下角色：

### 系统管理员

* 管理系统配置；
* 管理模型接口；
* 查看任务和日志；
* 处理异常任务。

### 教师

* 创建考试；
* 上传试卷和答案；
* 建立并确认模板；
* 上传学生试卷；
* 审核自动判分；
* 导出成绩。

### 审核教师

* 复核低置信度题目；
* 修改评分结果；
* 确认最终成绩。

后期可以扩展：

* 学校管理员；
* 年级管理员；
* 学生；
* 家长。

---

# 4. 核心设计原则

## 4.1 模板驱动，而不是整卷盲目识别

系统不直接让大模型每次重新理解整张学生试卷。

老师首先上传空白试卷，系统建立机器可读的试卷模板。模板记录：

* 每一道题的位置；
* 学生答案区域；
* 题号；
* 题型；
* 分值；
* 标准答案；
* 评分点；
* 判分方式。

学生试卷上传后，系统先与模板对齐，再按照模板裁出各题答案。

---

## 4.2 AI 自动建立，教师最终确认

模板不要求教师从零手动画框。

系统通过 OCR、版面分析、视觉大模型和规则检查自动生成模板草稿。教师只检查：

* AI 没有识别出的题目；
* 分值不确定的题目；
* 答案区域错误的题目；
* 题型识别错误的题目；
* 低置信度评分点。

模板必须由教师确认后才能投入批改。

---

## 4.3 大模型只负责适合的任务

不让大模型承担所有工作。

### 规则和传统算法负责

* 页面边缘检测；
* 透视矫正；
* 模板配准；
* 标准答题卡识别；
* 单选题；
* 多选题；
* 判断题；
* 数字答案；
* 分值计算；
* 坐标变换。

### OCR 负责

* 印刷文字；
* 题号；
* 页码；
* 学号；
* 短文本；
* 清晰数字；
* 文本坐标。

### 视觉大模型负责

* 潦草手写；
* 主观题；
* 简答题；
* 复杂公式；
* 多行计算过程；
* 评分点匹配；
* 扣分原因；
* 低置信度答案二次识别。

---

## 4.4 自动评分只是建议，教师保留最终决定权

系统需要保留：

* 原始试卷图片；
* OCR 结果；
* 模型原始输出；
* 评分点命中证据；
* 模型建议得分；
* 教师修改结果；
* 修改原因；
* 最终分数；
* 使用的模型和提示词版本。

---

## 4.5 所有结果必须保留卷面坐标

OCR、评分和批注结果不能只有文字和分数，还必须关联原试卷坐标。

例如：

```json
{
  "question_id": "q12",
  "answer_text": "学生答案内容",
  "answer_bbox": [0.12, 0.43, 0.88, 0.65],
  "suggested_score": 6,
  "max_score": 8,
  "deductions": [
    {
      "reason": "缺少单位",
      "score": -1,
      "bbox": [0.61, 0.58, 0.82, 0.63]
    }
  ]
}
```

这样批注才能准确回写到卷面。

---

# 5. 总体技术架构

## 5.1 系统架构

```text
React Web 前端
├── 考试管理
├── AI 试卷标定
├── 模板修正
├── 答卷上传
├── 教师复核
└── 批注编辑
          │
          ▼
FastAPI 业务后端
├── 用户和权限
├── 考试管理
├── 模板管理
├── 答卷管理
├── 判分结果
├── 审核记录
└── 导出管理
          │
          ▼
后台任务系统
├── PDF 渲染
├── 图像矫正
├── 页面配准
├── 题目裁剪
├── OCR
├── 大模型判分
└── 批注渲染
          │
          ▼
PostgreSQL + 文件存储
```

---

## 5.2 前端技术栈

* React；
* Vite；
* TypeScript；
* Tailwind CSS；
* shadcn/ui；
* TanStack Query；
* Zustand；
* React Konva；
* PDF.js。

### React Konva 的用途

同一个 Canvas 编辑器承担：

* 页面四角调整；
* 题目区域框选；
* 答案区域框选；
* 区域移动和缩放；
* 勾、叉、文字和扣分批注；
* 缩放和平移；
* 撤销和重做。

---

## 5.3 后端技术栈

* FastAPI；
* SQLModel；
* Pydantic；
* PostgreSQL；
* Alembic。

后端基于 Full Stack FastAPI Template 建立，保留其：

* 用户登录；
* JWT 认证；
* PostgreSQL；
* React 前端基础结构；
* Docker Compose；
* API Client；
* 自动化测试；
* CI 配置。

---

## 5.4 后台任务

建议采用：

* Dramatiq；
* Redis。

MVP 阶段设置两个 Worker。

### CPU Worker

负责：

* PDF 转图片；
* 图像质量检测；
* 页面边缘识别；
* 透视矫正；
* 模板配准；
* 单题裁剪；
* 标准答题卡识别；
* 基础 OCR；
* 批注渲染。

### Model Worker

负责：

* 视觉大模型识别；
* 主观题判分；
* 评分点匹配；
* 评语生成；
* 低置信度复核。

---

## 5.5 图像和 PDF 技术栈

* OpenCV；
* NumPy；
* Pillow；
* pypdfium2。

内部统一将 PDF 和照片转换为标准页面图片：

```text
PDF或照片
    ↓
标准页面图片
    ↓
模板坐标系
    ↓
题目和答案裁剪图
```

---

## 5.6 OCR 与模型

### OCR

采用 PaddleOCR，主要负责：

* 题号；
* 页码；
* 印刷文字；
* 学号；
* 数字；
* 简单短文本；
* 文本坐标；
* OCR 置信度。

### 视觉大模型

建立统一的模型适配层：

```python
class VisionModelAdapter:
    async def analyze_template(self, pages, answer_files):
        ...

    async def transcribe_answer(self, answer_image, question):
        ...

    async def grade_answer(self, question, answer, rubric):
        ...
```

业务系统不直接绑定某个模型厂商。

---

## 5.7 文件存储

开发环境：

* 本地文件目录；
* Docker Volume。

正式环境：

* S3；
* Cloudflare R2；
* 阿里云 OSS；
* 腾讯云 COS。

数据库只保存文件键，不直接保存大体积图片。

---

# 6. 开源项目使用方案

## 6.1 直接作为基础

### Full Stack FastAPI Template

作为主项目底座，提供：

* Web 前端；
* FastAPI 后端；
* 用户认证；
* PostgreSQL；
* Docker；
* 测试和部署配置。

---

## 6.2 作为依赖使用

### React Konva

用于：

* 页面四角编辑；
* 模板框选；
* 批注绘制；
* 教师复核。

### PaddleOCR

用于：

* OCR；
* 坐标提取；
* 数字和短文本识别。

### OpenCV

用于：

* 页面检测；
* 透视矫正；
* 配准；
* 标准答题卡识别；
* 图像增强。

### pypdfium2

用于：

* PDF 转页面图片。

---

## 6.3 作为算法参考

### OMRChecker

只参考其：

* 标准填涂答题卡识别；
* 填涂率计算；
* 选项判定。

不采用其完整业务架构。

---

## 6.4 后期参考

### MakeACopy

在开发移动端 App 时，参考其：

* 手机扫描；
* 自动边缘检测；
* 透视矫正；
* 图像增强；
* 多页扫描。

---

# 7. 主要业务流程

## 7.1 创建考试

教师填写：

* 考试名称；
* 科目；
* 年级；
* 班级；
* 总分；
* 考试日期。

然后上传：

* 空白试卷；
* 标准答案；
* 可选的评分细则。

---

## 7.2 AI 一键建立试卷模板

系统执行：

```text
试卷 PDF/图片
    ↓
PDF 渲染和图像标准化
    ↓
OCR 获取文字和题号坐标
    ↓
版面结构分析
    ↓
视觉大模型理解题目层级
    ↓
推断题型、答题区域和分值
    ↓
结合标准答案生成评分规则
    ↓
规则引擎检查
    ↓
生成模板草稿
```

AI 自动识别：

* 页面顺序；
* 页码；
* 姓名和学号区域；
* 大题和小题；
* 题号；
* 题干区域；
* 答案区域；
* 题型；
* 分值；
* 标准答案；
* 评分点；
* 建议的判分方式。

---

## 7.3 模板自动校验

系统生成模板后，执行以下检查。

### 题号检查

* 是否有缺号；
* 是否有重复题号；
* 子题层级是否合理。

### 分值检查

* 分值总和是否等于试卷总分；
* 是否有题目没有设置分值；
* 评分点分值是否等于题目满分。

### 区域检查

* 是否存在区域重叠；
* 是否超出页面；
* 是否有题目没有答案区域；
* 是否存在过大的异常区域；
* 题号和答案区域距离是否异常。

### 页面检查

* 是否缺页；
* 页面顺序是否错误；
* 是否存在重复页面。

---

## 7.4 教师修正模板

系统按置信度显示不同状态：

* 绿色：高置信度；
* 黄色：建议检查；
* 红色：必须确认；
* 灰色：未识别，需要补充。

教师可以：

* 拖动和调整区域；
* 增加题目；
* 删除错误题目；
* 拆分或合并题目；
* 修改题号；
* 修改题型；
* 修改答案区域；
* 修改分值；
* 修改标准答案；
* 修改评分点；
* 修改判分方式。

系统提供：

> 只查看待确认项

教师确认后，模板进入“已发布”状态。

---

## 7.5 上传学生答卷

支持：

* 单份上传；
* 批量上传；
* PDF；
* JPG；
* PNG。

系统自动识别：

* 学生姓名；
* 学号；
* 页面数量；
* 页面顺序。

识别不到时允许教师手动关联学生。

---

## 7.6 页面预处理与配准

每页学生答卷执行：

1. 图像质量检测；
2. 页面边缘检测；
3. 透视矫正；
4. 自动旋转；
5. 去阴影和亮度处理；
6. 与空白模板配准；
7. 计算变换矩阵；
8. 配准质量评分；
9. 按模板裁剪各题答案。

配准失败的页面进入人工修正。

---

## 7.7 按题型识别与评分

### 单选题

支持：

* 标准涂卡；
* 方框勾选；
* 圈选选项；
* 手写字母。

### 多选题

识别选项集合，并与标准选项集合比较。

### 判断题

识别：

* 勾；
* 叉；
* 正确或错误；
* 对或错。

### 数字填空题

支持：

* 整数；
* 小数；
* 分数；
* 百分数；
* 科学计数法；
* 正负号；
* 数值容差；
* 单位换算。

### 短文本题

支持：

* 精确匹配；
* 同义表达；
* 关键词匹配；
* 大模型语义复核。

### 主观题和计算题

大模型按评分点输出：

* 是否命中评分点；
* 对应学生答案证据；
* 每个评分点得分；
* 扣分原因；
* 建议总分；
* 置信度；
* 是否需要人工复核。

---

## 7.8 教师复核

复核页面采用左右结构：

```text
┌─────────────────────────┬─────────────────────┐
│                         │ 题号、题型、满分    │
│     学生原始试卷         │ OCR结果             │
│                         │ 标准答案            │
│     自动批注图层         │ 评分点              │
│                         │ 模型证据            │
│                         │ 建议得分            │
│                         │ 教师最终得分        │
└─────────────────────────┴─────────────────────┘
```

教师可以：

* 确认模型结果；
* 修改分数；
* 修改评分点；
* 修改扣分原因；
* 增加批注；
* 删除错误批注；
* 保存修改原因。

---

## 7.9 批注与导出

批注类型包括：

* 红勾；
* 红叉；
* 圈选；
* 下划线；
* 箭头；
* 扣分；
* 文字评语；
* 单题得分；
* 总分。

批注以 JSON 保存，不直接破坏原图。

系统可导出：

* 带批注的 PDF；
* 单页批改图片；
* 学生成绩表；
* 每题得分表；
* 班级错题统计；
* CSV 或 Excel 文件。

---

# 8. 核心数据模型

## 8.1 用户与教学组织

* User；
* School；
* Classroom；
* Student；
* Teacher。

MVP 可以先简化 School 和 Classroom，只实现教师与学生。

---

## 8.2 考试和模板

* Exam；
* PaperTemplate；
* TemplatePage；
* Question；
* QuestionRegion；
* RubricItem；
* TemplateGenerationTask；
* TemplateValidationIssue。

---

## 8.3 学生答卷

* Submission；
* SubmissionPage；
* AnswerCrop；
* PageTransform；
* ImageQualityResult。

---

## 8.4 识别与判分

* RecognitionResult；
* GradingResult；
* RubricMatch；
* ModelInvocation；
* ConfidenceResult。

---

## 8.5 批注和复核

* Annotation；
* ReviewTask；
* ReviewEvent；
* ScoreRevision；
* ExportRecord。

---

# 9. 任务状态设计

## 9.1 模板建立状态

```text
UPLOADED
    ↓
ANALYZING
    ↓
DRAFT_GENERATED
    ↓
VALIDATING
    ↓
REVIEW_REQUIRED
    ↓
CONFIRMED
    ↓
PUBLISHED
```

异常状态：

```text
ANALYSIS_FAILED
VALIDATION_FAILED
```

---

## 9.2 学生答卷处理状态

```text
UPLOADED
    ↓
PREPROCESSING
    ↓
ALIGNED
    ↓
SEGMENTED
    ↓
RECOGNIZING
    ↓
GRADING
    ↓
REVIEW_REQUIRED
    ↓
APPROVED
    ↓
EXPORTED
```

异常状态：

* PREPROCESS_FAILED；
* ALIGNMENT_FAILED；
* SEGMENTATION_FAILED；
* RECOGNITION_FAILED；
* GRADING_FAILED；
* EXPORT_FAILED。

每一步必须：

* 可以重试；
* 保留输入和输出；
* 保证幂等；
* 记录处理版本；
* 避免重复调用收费模型。

---

# 10. 开发周期安排

## 开发周期 0：项目初始化与技术底座

### 建议时长

1 周。

### 开发目标

完成主项目、数据库、任务系统和开发环境搭建。

### 主要任务

1. 基于 Full Stack FastAPI Template 创建项目；
2. 删除示例业务模块；
3. 建立前后端 Monorepo；
4. 配置 PostgreSQL；
5. 配置 Redis 和 Dramatiq；
6. 配置本地文件存储；
7. 建立数据库迁移；
8. 建立统一日志；
9. 建立错误处理；
10. 建立第三方许可证清单；
11. 定义坐标系统；
12. 定义文件目录规范；
13. 定义任务状态机；
14. 完成用户登录。

### 交付物

* 可运行的 Web 项目；
* FastAPI 后端；
* PostgreSQL；
* Redis；
* Worker；
* 登录页面；
* Docker Compose；
* API 文档；
* 项目开发规范。

### 验收标准

* 系统能够一键启动；
* 用户可以登录；
* 后端能够创建测试考试；
* Worker 能执行测试任务；
* 文件能够上传和读取。

---

## 开发周期 1：人工试卷标定基础能力

### 建议时长

2 周。

### 开发目标

先保证不依赖 AI 时，教师也能建立完整模板。

### 主要任务

1. 创建考试；
2. 上传空白试卷；
3. 支持 PDF、JPG 和 PNG；
4. 将 PDF 转为页面图片；
5. 开发 React Konva 页面编辑器；
6. 支持框选答案区域；
7. 支持框选题目区域；
8. 支持姓名、学号和页码区域；
9. 设置题号；
10. 设置题型；
11. 设置满分；
12. 设置标准答案；
13. 设置评分点；
14. 保存归一化坐标；
15. 模板保存、预览和修改；
16. 模板发布和版本管理。

### 交付物

* 试卷上传页面；
* 页面预览；
* 人工模板编辑器；
* 题目配置表单；
* 模板保存和发布功能。

### 验收标准

* 教师可以手动建立一套完整模板；
* 页面缩放后区域坐标不偏移；
* 模板关闭后重新打开可以完整恢复；
* 每道题可以设置答案和分值；
* 主观题可以设置评分点。

---

## 开发周期 2：AI 一键建立试卷模板

### 建议时长

2～3 周。

### 开发目标

实现上传试卷后自动生成模板草稿，教师只修正异常项。

### 主要任务

1. 接入 PaddleOCR；
2. 获取文字、题号和坐标；
3. 识别页面结构；
4. 识别大题和小题层级；
5. 识别选项和答题横线；
6. 推断答案区域；
7. 推断题型；
8. 提取分值；
9. 解析标准答案文件；
10. 解析评分细则；
11. 调用视觉大模型理解复杂版面；
12. 生成模板 JSON；
13. 为每项结果计算置信度；
14. 标记高、中、低置信度；
15. 检查题号连续性；
16. 检查分值总和；
17. 检查区域重叠和越界；
18. 开发“只看待确认项”功能；
19. 支持教师修改 AI 模板；
20. 保存 AI 原始结果和教师修正结果。

### 交付物

* AI 一键标定按钮；
* 模板生成任务；
* 自动题目和答案区域；
* 自动题型和分值识别；
* 置信度显示；
* 模板问题列表；
* 教师纠错界面。

### 验收标准

* 系统可以生成可编辑模板草稿；
* AI 结果不会直接发布；
* 分值不确定时明确提示；
* 低置信度项目必须教师确认；
* 教师可以修改所有 AI 结果；
* 修改后模板能够正常发布。

---

## 开发周期 3：学生答卷处理与模板配准

### 建议时长

2 周。

### 开发目标

实现学生答卷上传、页面矫正、模板配准和单题裁剪。

### 主要任务

1. 单份和批量上传；
2. PDF 和图片支持；
3. PDF 页面渲染；
4. 模糊度检测；
5. 曝光检测；
6. 分辨率检查；
7. 页面边缘检测；
8. 人工四角修正；
9. 透视矫正；
10. 页面旋转；
11. 页面顺序判断；
12. 与空白模板配准；
13. 保存 Homography；
14. 计算配准质量；
15. 按模板裁剪答案；
16. 失败任务重试；
17. 任务进度显示；
18. 学生身份关联。

### 交付物

* 答卷上传模块；
* 图像质量检测；
* 页面矫正；
* 模板配准；
* 单题答案裁剪；
* 异常页面人工处理界面。

### 验收标准

* 倾斜拍摄的页面能够矫正；
* 学生卷能够与模板对齐；
* 题目裁剪区域完整；
* 配准失败可以人工修正；
* 失败任务不需要重新上传。

---

## 开发周期 4：客观题与基础 OCR 判分

### 建议时长

2 周。

### 开发目标

完成选择题、判断题、数字填空和短文本题的自动评分。

### 主要任务

1. OCR Worker；
2. 保存文字、坐标和置信度；
3. 题型判分路由；
4. 单选题识别；
5. 多选题识别；
6. 判断题识别；
7. 标准答题卡识别；
8. 手写选项字母识别；
9. 数值规范化；
10. 分数、小数和百分数转换；
11. 数值容差；
12. 单位规范化；
13. 短文本精确匹配；
14. 关键词匹配；
15. 低置信度复核；
16. 自动生成基础勾叉和得分。

### 交付物

* 客观题判分引擎；
* 数字答案引擎；
* OCR 结果页面；
* 低置信度队列；
* 基础批注结果。

### 验收标准

* 标准选择题能够自动判分；
* 数字答案能处理常见等价表达；
* OCR 原始结果可以查看；
* 低置信度结果不会直接确认；
* 判分结果不能超过题目满分。

---

## 开发周期 5：主观题和视觉大模型判分

### 建议时长

3 周。

### 开发目标

实现手写答案识别、评分点匹配和主观题建议评分。

### 主要任务

1. 统一 Vision Model Adapter；
2. 配置模型供应商；
3. 发送匿名单题裁剪图；
4. 识别手写答案；
5. 恢复多行文本；
6. 识别简单公式；
7. 输入标准答案和评分点；
8. 按评分点逐项判定；
9. 要求输出证据；
10. 强制固定 JSON Schema；
11. 检查总分范围；
12. 生成扣分原因；
13. 生成简短评语；
14. 计算模型置信度；
15. 保存模型名称和版本；
16. 保存提示词版本；
17. 保存原始模型输出；
18. 模型失败时自动进入人工复核；
19. 建立模型调用缓存；
20. 统计调用成本。

### 交付物

* 大模型适配层；
* 主观题判分模块；
* 评分点匹配结果；
* 模型证据；
* 建议分数；
* 评语建议；
* 模型调用记录。

### 验收标准

* 模型必须按评分点输出；
* 每个得分有相应证据；
* 模型不能自行更改满分；
* 输出格式错误不能进入最终成绩；
* 主观题默认进入教师确认；
* 重试不会重复调用已成功模型结果。

---

## 开发周期 6：教师复核、批注回写与导出

### 建议时长

2 周。

### 开发目标

完成教师审核、卷面批注、PDF 导出和成绩统计。

### 主要任务

1. 教师复核工作台；
2. 原卷和模型结果并排显示；
3. 修改单题分数；
4. 修改评分点；
5. 修改扣分原因；
6. 保存修改原因；
7. React Konva 批注图层；
8. 红勾和红叉；
9. 圈选和下划线；
10. 扣分文字；
11. 题目得分；
12. 总分；
13. 批注移动和删除；
14. 批注 JSON 保存；
15. 后端批注渲染；
16. 生成批改后 PDF；
17. 导出成绩表；
18. 导出每题得分；
19. 错题统计；
20. 重新导出不重复判分。

### 交付物

* 教师复核页面；
* 批注编辑器；
* PDF 导出；
* 成绩统计；
* CSV 或 Excel 导出；
* 审核记录。

### 验收标准

* 批注位置准确；
* 不同缩放比例下批注不偏移；
* 教师可以修改模型结果；
* 导出 PDF 与网页显示一致；
* 总分和各题得分计算正确；
* 每次人工修改均有记录。

---

## 开发周期 7：评测、试点和生产部署

### 建议时长

2 周。

### 开发目标

建立评测集，完成真实小规模试点并准备正式部署。

### 主要任务

1. 建立脱敏试卷测试集；
2. 覆盖清晰、模糊、倾斜和阴影图片；
3. 覆盖不同学生字迹；
4. 覆盖选择、填空和主观题；
5. 评测页面配准；
6. 评测答案裁剪；
7. 评测 OCR；
8. 评测判分一致性；
9. 统计人工复核率；
10. 统计严重误判率；
11. 统计处理耗时；
12. 统计模型成本；
13. 优化置信度阈值；
14. 权限检查；
15. 隐私检查；
16. 数据备份；
17. 日志和监控；
18. 生产部署文档；
19. 选择一套真实试卷试点；
20. 收集教师反馈。

### 交付物

* 测试数据集；
* 系统评测报告；
* 模型选型报告；
* 试点报告；
* 生产部署方案；
* 下一版本需求清单。

### 验收标准

* 完成一套试卷的完整批改；
* 页面配准达到可用水平；
* 客观题评分稳定；
* 主观题结果可解释；
* 异常题能进入人工审核；
* 教师能够完成真实试点。

---

# 11. 周期与里程碑汇总

| 周期   | 核心内容       |  建议时长 |
| ---- | ---------- | ----: |
| 周期 0 | 项目初始化与技术底座 |   1 周 |
| 周期 1 | 人工试卷标定     |   2 周 |
| 周期 2 | AI 一键建立模板  | 2～3 周 |
| 周期 3 | 答卷处理与模板配准  |   2 周 |
| 周期 4 | 客观题与基础 OCR |   2 周 |
| 周期 5 | 主观题大模型判分   |   3 周 |
| 周期 6 | 教师复核、批注与导出 |   2 周 |
| 周期 7 | 评测、试点与部署   |   2 周 |

完整开发约需 16 周。

前后端和算法开发适当并行后，可以压缩到 14 周左右。

---

# 12. MVP 范围

## 12.1 MVP 必须实现

* 用户登录；
* 考试管理；
* 空白试卷上传；
* 标准答案上传；
* 人工模板标定；
* AI 一键建立模板；
* 模板置信度和异常提示；
* 教师模板修正；
* 学生答卷批量上传；
* 页面矫正；
* 模板配准；
* 单题裁剪；
* 客观题评分；
* 基础 OCR；
* 主观题建议评分；
* 教师人工复核；
* 红笔批注；
* PDF 导出；
* 成绩表导出；
* 操作审计。

---

## 12.2 MVP 暂不实现

* 任意陌生试卷完全自动理解；
* 无模板批卷；
* 作文自动最终评分；
* 高难数学证明题完整步骤评分；
* 浏览器实时自动拍照；
* 多学校 SaaS；
* 学生端 App；
* 离线模型训练；
* OCR 模型微调；
* 向量数据库；
* 知识图谱；
* 复杂多租户计费；
* 大规模微服务架构。

---

# 13. 评测指标

## 13.1 模板生成

* 题号识别准确率；
* 题型识别准确率；
* 答案区域覆盖率；
* 分值识别准确率；
* AI 模板人工修改率；
* 模板建立平均耗时。

## 13.2 页面处理

* 页面检测成功率；
* 透视矫正成功率；
* 模板配准成功率；
* 答案裁剪完整率；
* 页面人工干预率。

## 13.3 OCR 与判分

* OCR 字符错误率；
* 客观题判分准确率；
* 数字题判分准确率；
* 主观题与教师评分一致率；
* 平均分差；
* 严重误判率；
* 人工复核率。

## 13.4 系统效率

* 每页处理时间；
* 每份试卷处理时间；
* 每份试卷模型调用成本；
* 教师平均复核时间；
* 相比纯人工批改节省的时间。

---

# 14. 主要风险与应对措施

## 14.1 AI 模板区域识别错误

应对措施：

* AI 结果只生成草稿；
* 显示置信度；
* 自动检查题号和分值；
* 提供人工编辑；
* 保留人工模板模式。

---

## 14.2 学生答卷与模板配准失败

应对措施：

* 保留原始图；
* 支持人工四角调整；
* 设置配准质量评分；
* 使用定位点或页码辅助；
* 失败页面进入人工修正。

---

## 14.3 手写 OCR 准确率不足

应对措施：

* 普通 OCR 与视觉模型分层；
* 只识别单题裁剪图；
* 保存识别置信度；
* 使用教师修正建立数据集；
* 低置信度答案人工复核。

---

## 14.4 主观题评分不一致

应对措施：

* 使用结构化评分点；
* 要求模型提供证据；
* 固定输出格式；
* 限制最高分；
* 主观题默认教师确认；
* 保存教师最终修改。

---

## 14.5 模型调用成本过高

应对措施：

* 客观题不使用大模型；
* OCR 优先；
* 只发送答案裁剪图；
* 对结果进行缓存；
* 失败重试不重复收费；
* 统计单题和单卷成本。

---

## 14.6 学生隐私风险

应对措施：

* 姓名和学号区域单独识别；
* 外部模型只接收匿名答案裁剪；
* 不向外部模型发送完整试卷；
* 保存模型请求审计；
* 后续支持本地模型。

---

## 14.7 开源许可证风险

应对措施：

* 建立 THIRD_PARTY_LICENSES 文件；
* 记录每个依赖项目的许可证；
* 不直接复制无许可证项目代码；
* 对参考算法进行独立模块化实现；
* 商业发布前再次进行许可证审核。

---

# 15. 后续 App 迁移路线

Web MVP 通过真实试点后，开发移动端 App。

## 15.1 App 第一阶段功能

* 登录；
* 选择考试；
* 手机拍照；
* 自动寻找试卷边缘；
* 人工调整四角；
* 连续扫描；
* 自动识别页码；
* 模糊和曝光提示；
* 本地临时保存；
* 上传试卷；
* 查看处理进度；
* 查看批改结果。

---

## 15.2 App 技术栈

推荐：

* React Native；
* Expo；
* TypeScript；
* 原生相机模块；
* 原生 OpenCV 或扫描组件。

---

## 15.3 Web 和 App 可复用内容

* TypeScript 类型；
* API Client；
* 用户登录逻辑；
* 任务状态；
* 模板数据结构；
* 批注 Schema；
* 判分结果 Schema；
* 上传协议；
* 错误码体系。

---

# 16. 最终验收目标

MVP 完成后，系统应支持以下完整流程：

```text
教师创建考试
    ↓
上传空白试卷和标准答案
    ↓
AI 自动生成试卷模板
    ↓
教师只修正异常区域
    ↓
批量上传学生答卷
    ↓
自动完成页面矫正和模板配准
    ↓
自动裁剪各题答案
    ↓
规则、OCR和视觉大模型联合判分
    ↓
生成批注和分数建议
    ↓
教师复核并确认
    ↓
导出带批注试卷和成绩统计
```

项目第一阶段的最终成功标准是：

> 在固定试卷模板场景下，系统可以稳定完成试卷数字化、答案裁剪、自动评分建议、教师复核和批注导出，并明显减少教师的重复批改时间。

同时，系统积累以下核心资产：

* 试卷模板数据；
* 学生答案裁剪数据；
* OCR 结果；
* 模型评分结果；
* 教师修正记录；
* 评分点命中数据；
* 不同题型的真实评测集。

这些数据将为后续提升模型准确率、开发 App、支持更多学科和实现更高程度自动化奠定基础。

这版可以直接作为立项方案、开发任务总纲或 GitHub Milestone 拆分依据。
